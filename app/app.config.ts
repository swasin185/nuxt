export default defineAppConfig({
    ui: {
        colorMode: false,
        table: {
            slots: {
                td: 'm-1 p-1 cursor-pointer',
                th: 'p-2',
            },
        },
    },
})
